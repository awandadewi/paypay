<!--  Navbar header -->
<nav class="navbar navbar-expand navbar-light flex-column flex-md-row kw-header justify-content-between">
    <%= link_to "Paypay", root_path, class: "navbar-brand" %>
    <div class="">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Account
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <a href="javascript:void();" class="dropdown-item"><%= current_user.try(:company).try(:name) || "No company" %></a>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <form class="form-inline">
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "btn btn-link my-2 my-sm-0 mr-2" %>
            </li>
            <li class="nav-item">
              <%= link_to "Register", root_path, class: "btn btn-primary my-2 my-sm-0" %>
            </li>
          </form>
        <% end %>
      </ul>
    </div>
  </nav>
